import { ParaHighlighterDirective } from './para-highlighter.directive';

describe('ParaHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new ParaHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
